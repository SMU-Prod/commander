# Onda 4 — O Mapa Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Um mapa Mapbox com duas caras: vitrine de parceiros comerciais (marina/posto/pousada/restaurante, autoatendimento) e navegador de bordo degrau 1 (posição com rumo, SOG, destino com distância/ETA em MN, alarme de âncora, MOB, overlay OpenSeaMap).

**Architecture:** `/navegar` vira a tela do mapa (server component busca parceiros → client component Mapbox), absorvendo a gravação de trilha que já existe ali — nada de funcionalidade perdida. Parceiro é um 4º papel: usuário comum com linha em `parceiros` e painel próprio em `/parceiro` (fora do grupo `(app)`), que posiciona o próprio pino arrastando. Sem token Mapbox tudo degrada com mensagem, nunca com crash (padrão dos envs opcionais).

**Tech Stack:** mapbox-gl (GL JS v3), OpenSeaMap raster overlay (CC-BY-SA, atribuição obrigatória), Supabase (RLS + bucket público `parceiros`), domínio puro TDD.

## Global Constraints

- Sem `NEXT_PUBLIC_MAPBOX_TOKEN`: build/testes verdes e telas com aviso amigável — nunca crash.
- Conteúdo de parceiro é NÃO-CONFIÁVEL: nunca `Popup.setHTML`; cards renderizados pelo React.
- Toda escrita Supabase usa `.select()` + checagem de linhas (padrão da casa).
- Disclaimer visível no mapa: "Auxílio à navegação — não substitui as cartas náuticas oficiais."
- Atribuição no mapa: "© Mapbox © OpenStreetMap © OpenSeaMap".
- Preços em centavos; PT-BR náutico; tipografia sem cor (`text-dim`); `sombra-1/2`; alvos ≥44px; ícones via `<Icone>`.
- Migrations aplicadas via MCP E versionadas idênticas.
- Commits PT-BR sem acento no assunto; hook de pré-commit nunca pulado.

---

### Task 1: Migration 020 — parceiros + bucket + tipos

**Files:**
- Create: `supabase/migrations/020_parceiros.sql`
- Modify: `web/lib/db/types.ts` (fim)

**Interfaces:**
- Produces: tabela `parceiros`, RPC `registrar_visualizacao(uuid)`, bucket público `parceiros`, tipos `Parceiro`/`CategoriaParceiro`/`PlanoParceiro`.

- [ ] **Step 1: Migration** — aplicar via MCP (`apply_migration`, projeto `khgjtxvmduizyooqaoox`, conector `mcp__6dcbebfb-...`, NUNCA plugin_supabase) e versionar o MESMO SQL:

```sql
-- 020: parceiros comerciais no mapa (espec v3 §11 + conversa 07/08).
-- Autoatendimento: o parceiro e um usuario comum com linha aqui e painel proprio.
-- tem_poita/qtd_poitas sao o gancho da onda 9 (aluguel de poita) — so exibicao por ora.

create table public.parceiros (
  id uuid primary key default gen_random_uuid(),
  usuario_id uuid not null unique references auth.users(id) on delete cascade,
  categoria text not null check (categoria in ('marina','posto','pousada','restaurante')),
  nome text not null,
  sobre text,
  telefone text,
  email text,
  horario text,
  lat double precision not null check (lat between -90 and 90),
  lng double precision not null check (lng between -180 and 180),
  preco_diaria_centavos integer check (preco_diaria_centavos > 0),
  preco_diesel_centavos integer check (preco_diesel_centavos > 0),
  calado_max_m numeric,
  tem_poita boolean not null default false,
  qtd_poitas integer check (qtd_poitas >= 0),
  traslado_incluso boolean,
  vaga_cortesia boolean,
  culinaria text,
  plano text not null default 'cortesia' check (plano in ('cortesia','basico','destaque')),
  visivel boolean not null default true,
  fotos text[] not null default '{}' check (coalesce(array_length(fotos, 1), 0) <= 3),
  visualizacoes integer not null default 0,
  precos_atualizados_em timestamptz,
  criado_em timestamptz not null default now(),
  atualizado_em timestamptz not null default now()
);

create index parceiros_categoria_idx on public.parceiros (categoria) where visivel;

alter table public.parceiros enable row level security;

create policy "parceiro: ver visiveis ou o proprio" on public.parceiros
  for select to authenticated
  using (visivel or usuario_id = (select auth.uid()));

create policy "parceiro: criar o proprio" on public.parceiros
  for insert to authenticated
  with check (usuario_id = (select auth.uid()) and plano = 'cortesia' and visualizacoes = 0);

create policy "parceiro: editar o proprio" on public.parceiros
  for update to authenticated
  using (usuario_id = (select auth.uid()))
  with check (usuario_id = (select auth.uid()));

-- plano quem muda e a operacao (service_role); visualizacoes so via RPC;
-- precos_atualizados_em so via trigger. Privilegio de coluna trava o resto.
revoke update on table public.parceiros from authenticated;
grant update (categoria, nome, sobre, telefone, email, horario, lat, lng,
  preco_diaria_centavos, preco_diesel_centavos, calado_max_m, tem_poita, qtd_poitas,
  traslado_incluso, vaga_cortesia, culinaria, visivel, fotos)
  on table public.parceiros to authenticated;

-- regra da espec: 1 atualizacao de preco/disponibilidade por dia
create or replace function public.parceiro_regras()
returns trigger language plpgsql security definer set search_path = public as $$
begin
  if (new.preco_diaria_centavos is distinct from old.preco_diaria_centavos)
     or (new.preco_diesel_centavos is distinct from old.preco_diesel_centavos)
     or (new.qtd_poitas is distinct from old.qtd_poitas) then
    if old.precos_atualizados_em is not null
       and old.precos_atualizados_em::date = current_date then
      raise exception 'limite de 1 atualizacao de preco/disponibilidade por dia';
    end if;
    new.precos_atualizados_em := now();
  else
    new.precos_atualizados_em := old.precos_atualizados_em;
  end if;
  new.plano := old.plano;
  new.visualizacoes := old.visualizacoes;
  new.atualizado_em := now();
  return new;
end $$;
create trigger parceiros_regras before update on public.parceiros
  for each row execute function public.parceiro_regras();
revoke execute on function public.parceiro_regras() from public, anon, authenticated;

-- contador de visualizacoes (metrica de renovacao do parceiro)
create or replace function public.registrar_visualizacao(p_parceiro_id uuid)
returns void language sql security definer set search_path = public as $$
  update public.parceiros set visualizacoes = visualizacoes + 1
  where id = p_parceiro_id and visivel;
$$;
revoke all on function public.registrar_visualizacao(uuid) from public, anon;
grant execute on function public.registrar_visualizacao(uuid) to authenticated;

-- bucket publico de fotos do parceiro (conteudo de marketing; leitura aberta,
-- escrita so na propria pasta <usuario_id>/...)
insert into storage.buckets (id, name, public) values ('parceiros', 'parceiros', true)
  on conflict (id) do nothing;
create policy "parceiros: subir na propria pasta" on storage.objects
  for insert to authenticated
  with check (bucket_id = 'parceiros' and (storage.foldername(name))[1] = (select auth.uid())::text);
create policy "parceiros: apagar da propria pasta" on storage.objects
  for delete to authenticated
  using (bucket_id = 'parceiros' and (storage.foldername(name))[1] = (select auth.uid())::text);
```

- [ ] **Step 2: Tipos** — fim de `web/lib/db/types.ts`:

```ts
export type CategoriaParceiro = "marina" | "posto" | "pousada" | "restaurante"
export type PlanoParceiro = "cortesia" | "basico" | "destaque"

export interface Parceiro {
  id: string
  usuario_id: string
  categoria: CategoriaParceiro
  nome: string
  sobre: string | null
  telefone: string | null
  email: string | null
  horario: string | null
  lat: number
  lng: number
  preco_diaria_centavos: number | null
  preco_diesel_centavos: number | null
  calado_max_m: number | null
  tem_poita: boolean
  qtd_poitas: number | null
  traslado_incluso: boolean | null
  vaga_cortesia: boolean | null
  culinaria: string | null
  plano: PlanoParceiro
  visivel: boolean
  fotos: string[]
  visualizacoes: number
  precos_atualizados_em: string | null
  criado_em: string
  atualizado_em: string
}
```

- [ ] **Step 3: Verificar e comitar** — `get_advisors(security)` sem novidade além do esperado (`registrar_visualizacao` security definer é intencional); testar no banco (transação revertida): parceiro não muda o próprio `plano` nem `visualizacoes` via UPDATE; segunda atualização de preço no mesmo dia falha. `npm test` 100, tsc. Commit: `feat: migration dos parceiros comerciais`

---

### Task 2: Domínio de navegação (TDD)

**Files:**
- Create: `web/lib/domain/navegacao.ts` + `web/lib/domain/navegacao.test.ts`

**Interfaces:**
- Consumes: `haversineNm` de `@/lib/domain/geo`.
- Produces: `msParaNos`, `rumoGraus`, `etaMinutos`, `foraDoRaio`, `tempoDesde` — usados nas Tasks 5-7.

- [ ] **Step 1: Testes primeiro** — `navegacao.test.ts`:

```ts
import { describe, expect, it } from "vitest"
import { etaMinutos, foraDoRaio, msParaNos, rumoGraus, tempoDesde } from "./navegacao"

describe("navegacao", () => {
  it("converte m/s do GPS para nos", () => {
    expect(msParaNos(10)).toBeCloseTo(19.44, 1)
    expect(msParaNos(0)).toBe(0)
    expect(msParaNos(null)).toBeNull()
  })
  it("rumo verdadeiro entre dois pontos (0=N, 90=E, 180=S, 270=W)", () => {
    expect(rumoGraus({ la: 0, lo: 0 }, { la: 1, lo: 0 })).toBeCloseTo(0, 0)
    expect(rumoGraus({ la: 0, lo: 0 }, { la: 0, lo: 1 })).toBeCloseTo(90, 0)
    expect(rumoGraus({ la: 0, lo: 0 }, { la: -1, lo: 0 })).toBeCloseTo(180, 0)
    expect(rumoGraus({ la: 0, lo: 0 }, { la: 0, lo: -1 })).toBeCloseTo(270, 0)
  })
  it("eta em minutos na velocidade atual; sem eta quase parado", () => {
    expect(etaMinutos(10, 20)).toBe(30)
    expect(etaMinutos(3.5, 7)).toBe(30)
    expect(etaMinutos(10, 0.4)).toBeNull()
    expect(etaMinutos(0, 10)).toBe(0)
  })
  it("alarme de ancora dispara fora do raio", () => {
    const ancora = { la: -23.0, lo: -44.3 }
    expect(foraDoRaio(ancora, ancora, 30)).toBe(false)
    // ~111m ao norte (0.001 grau de latitude)
    expect(foraDoRaio(ancora, { la: -22.999, lo: -44.3 }, 50)).toBe(true)
    expect(foraDoRaio(ancora, { la: -22.999, lo: -44.3 }, 200)).toBe(false)
  })
  it("tempoDesde legivel em pt-BR", () => {
    expect(tempoDesde("2026-08-07T10:00:00Z", "2026-08-07T10:30:00Z")).toBe("há 30 min")
    expect(tempoDesde("2026-08-07T07:00:00Z", "2026-08-07T10:00:00Z")).toBe("há 3 h")
    expect(tempoDesde("2026-08-04T10:00:00Z", "2026-08-07T10:00:00Z")).toBe("há 3 dias")
    expect(tempoDesde("2026-08-07T09:59:40Z", "2026-08-07T10:00:00Z")).toBe("agora há pouco")
  })
})
```

- [ ] **Step 2: RED**, **Step 3: implementar**:

```ts
import { haversineNm } from "@/lib/domain/geo"

/** Navegacao ponto-a-ponto do navegador de bordo. Puro e testavel. */

export function msParaNos(ms: number | null): number | null {
  if (ms == null) return null
  return ms * 1.9438445
}

/** Rumo verdadeiro inicial (great circle) em graus 0-360. */
export function rumoGraus(de: { la: number; lo: number }, para: { la: number; lo: number }): number {
  const f1 = (de.la * Math.PI) / 180
  const f2 = (para.la * Math.PI) / 180
  const dl = ((para.lo - de.lo) * Math.PI) / 180
  const y = Math.sin(dl) * Math.cos(f2)
  const x = Math.cos(f1) * Math.sin(f2) - Math.sin(f1) * Math.cos(f2) * Math.cos(dl)
  return (((Math.atan2(y, x) * 180) / Math.PI) + 360) % 360
}

/** ETA em minutos; null abaixo de meio no (parado/garrando, eta nao faz sentido). */
export function etaMinutos(distanciaNm: number, velKt: number): number | null {
  if (velKt < 0.5) return null
  return Math.round((distanciaNm / velKt) * 60)
}

export function foraDoRaio(
  ancora: { la: number; lo: number },
  atual: { la: number; lo: number },
  raioM: number,
): boolean {
  return haversineNm(ancora, atual) * 1852 > raioM
}

/** "ha 3 dias" / "ha 2 h" / "ha 30 min" / "agora ha pouco" — para o card do parceiro. */
export function tempoDesde(iso: string, agoraIso: string): string {
  const ms = new Date(agoraIso).getTime() - new Date(iso).getTime()
  const min = Math.floor(ms / 60_000)
  if (min < 5) return "agora há pouco"
  if (min < 60) return `há ${min} min`
  const h = Math.floor(min / 60)
  if (h < 24) return `há ${h} h`
  const d = Math.floor(h / 24)
  return d === 1 ? "há 1 dia" : `há ${d} dias`
}
```

- [ ] **Step 4: GREEN** (suíte 105) e commit: `feat: dominio de navegacao ponto-a-ponto (TDD)`

---

### Task 3: Painel do parceiro (autoatendimento)

**Files:**
- Create: `web/lib/acoes/parceiro.ts`, `web/app/(parceiro)/parceiro/page.tsx`, `web/app/(parceiro)/layout.tsx`, `web/components/mapa/escolher-ponto.tsx`

**Interfaces:**
- Consumes: `Parceiro`/tipos, `campo`/`rot` de `@/lib/ui/form`, `parseDecimalPtBr`, `<Icone>`, bucket `parceiros`.
- Produces: actions `salvarParceiro(formData)` (upsert cria/edita), `subirFotoParceiro`, `excluirFotoParceiro`; componente `<EscolherPonto lat lng>` reutilizado nunca mais (só aqui).

- [ ] **Step 1: `escolher-ponto.tsx`** — client component: com `NEXT_PUBLIC_MAPBOX_TOKEN`, renderiza um mapa Mapbox pequeno (h-64, rounded-[14px]) com UM `Marker({ draggable: true })`; `dragend` escreve em `<input type="hidden" name="lat|lng">` (e mostra as coordenadas em `apoio text-dim`). SEM token: dois inputs numéricos visíveis (lat/lng) com placeholder e dica "cole do Google Maps". Import de `mapbox-gl` e do CSS `mapbox-gl/dist/mapbox-gl.css` DENTRO deste componente client (se o build reclamar de CSS global, mover o import do CSS para `web/app/layout.tsx` — desvio esperado, documentar).
- [ ] **Step 2: actions** — `salvarParceiro`: upsert (se o usuário já tem linha, update; senão insert) com validação: categoria válida, nome ≥3, lat/lng nos ranges, preços via `parseDecimalPtBr` (reais → centavos ×100, aceita vírgula), erro legível se o trigger de 1/dia barrar (mensagem do Postgres contém 'limite de 1 atualizacao' → traduzir para "Preço só pode ser atualizado 1× por dia."). Fotos: `subirFotoParceiro` valida MIME (jpeg/png/webp), máx 3 (checa `fotos.length`), sobe em `parceiros/<usuario_id>/<uuid>.<ext>`, adiciona ao array; `excluirFotoParceiro` remove do array E do storage. TODAS as escritas com `.select()` + checagem. Helpers de erro `(): never` com `?erro=`.
- [ ] **Step 3: página `/parceiro`** — server: exige login (`redirect /login?volta=/parceiro`); busca a linha do usuário. Sem linha → form de cadastro (categoria como 4 cards-rádio com `<Icone>`, campos por categoria: pousada/restaurante mostram calado/traslado/cortesia/culinária; posto mostra preço diesel; todos mostram poita sim/não + quantas). Com linha → mesmo form preenchido + galeria das 3 fotos (URL pública do bucket) + toggle `visivel` + card de métricas ("<N> proprietários viram seu perfil"). Header próprio (grupo `(parceiro)` tem layout minimalista com wordmark, sem bottom-nav do app).
- [ ] **Step 4: Verificar e comitar** — testes 105, tsc, eslint, build (rota `/parceiro`), fluxo sem token não quebra. Commit: `feat: painel autoatendimento do parceiro comercial`

---

### Task 4: Componente do mapa náutico

**Files:**
- Create: `web/components/mapa/mapa-nautico.tsx`

**Interfaces:**
- Produces: `<MapaNautico aoIniciar={(map) => void} className>` — client component que: (1) sem token renderiza um painel navy com `<Icone nome="bussola">` e "O mapa precisa de configuração — adicione NEXT_PUBLIC_MAPBOX_TOKEN" (em dev) / "Mapa indisponível no momento" (produção); (2) com token inicializa `mapboxgl.Map` (style `mapbox://styles/mapbox/standard`, centro default Baía da Ilha Grande `[-44.14, -23.09]`, zoom 10), adiciona camada raster OpenSeaMap (`https://tiles.openseamap.org/seamark/{z}/{x}/{y}.png`, tileSize 256, atribuição "© OpenSeaMap"), `GeolocateControl` com `positionOptions: { enableHighAccuracy: true }`, `trackUserLocation: true`, `showUserHeading: true`, `fitBoundsOptions: { maxZoom: 14 }`, `NavigationControl` compacto, e chama `aoIniciar(map)` no `load`.

- [ ] **Step 1:** implementar como descrito; cleanup no unmount (`map.remove()`); disclaimer fixo no canto inferior: `rotulo` sobre pílula navy translúcida — "Auxílio à navegação — não substitui as cartas náuticas oficiais".
- [ ] **Step 2:** `npm install mapbox-gl`; build verde SEM o token. Commit: `feat: componente do mapa nautico com openseamap`

---

### Task 5: /navegar — o mapa com os parceiros (sem perder a trilha)

**Files:**
- Modify: `web/app/(app)/navegar/page.tsx` (vira server component: busca parceiros visíveis + repassa)
- Create: `web/components/mapa/navegar-mapa.tsx` (client — absorve TODO o código atual da página: watchPosition, painel de velocidade, gravação/salvar trilha via `salvarTrilha`)
- Create: `web/components/mapa/card-parceiro.tsx`

**Interfaces:**
- Consumes: `<MapaNautico>`, `Parceiro`, `tempoDesde`, RPC `registrar_visualizacao`.
- Produces: pinos por categoria + card bottom-sheet; estado `destino` exposto para a Task 6.

- [ ] **Step 1:** page.tsx server: `supabase.from("parceiros").select("*").eq("visivel", true)` → `<NavegarMapa parceiros={...} />`. NADA da UI atual de trilha some — ela migra para dentro do client component (ler o arquivo atual INTEIRO antes; a lógica de `pontosRef`/`resumoTrilha`/`salvarTrilha` é preservada, só muda de casa; o painel de trilha vira um cartão flutuante sobre o mapa, recolhível).
- [ ] **Step 2:** pinos: `Marker` com `element` custom — círculo `size-9` (36px ≥ alvo com padding do toque do mapa) fundo navy, `<Icone>` por categoria (`marina`→`ancora`, `posto`→`combustivel`, `pousada`→`cama`, `restaurante`→`talheres` — CONFIRA os 28 nomes reais em `icone.tsx` e escolha os mais próximos; não invente path). Plano `destaque` = anel dourado + z-index acima; `tem_poita` = ponto dourado no canto.
- [ ] **Step 3:** `card-parceiro.tsx` — bottom-sheet (fixed bottom, `sombra-2`, rounded-t-[20px], arrasta/X para fechar): nome + categoria + badges (Destaque/Poita com quantas), 3 fotos em fila (h-20, object-cover, URL pública), sobre, preços formatados (`formatarReais`), calado máx, traslado/cortesia quando houver, "atualizado <tempoDesde>" — e se >30 dias, aviso âmbar "informações podem estar desatualizadas" (regra da espec). Ações (h-11): `tel:` quando houver telefone, "Como chegar" → `https://www.google.com/maps/dir/?api=1&destination=<lat>,<lng>` (`target="_blank" rel="noopener noreferrer"`), "Traçar rumo" (chama callback da Task 6). Ao abrir o card: `supabase.rpc("registrar_visualizacao", ...)` fire-and-forget (client anon do browser — usuário está logado).
- [ ] **Step 4:** verificação + commit: `feat: mapa do navegar com parceiros e trilha preservada`

---

### Task 6: Navegação ativa — rumo, ETA, âncora e MOB

**Files:**
- Modify: `web/components/mapa/navegar-mapa.tsx`

**Interfaces:**
- Consumes: `msParaNos`, `rumoGraus`, `etaMinutos`, `foraDoRaio`, `haversineNm`.

- [ ] **Step 1: SOG sempre visível** — pílula no topo do mapa: `<velKt> kt` (fonte mono tabular, já existe o padrão do horímetro). Reusar o MESMO `watchPosition` da trilha (um só watcher alimenta trilha + SOG + âncora — nunca dois watchers).
- [ ] **Step 2: Traçar rumo** — `destino` setado pelo card do parceiro OU por long-press/toque no mapa (toque simples abre parceiro; toque em área vazia com modo "definir destino" ativado por botão). Com destino: source GeoJSON `rumo` com linha tracejada dourada posição→destino, painel compacto: distância `haversineNm` em MN (1 casa), rumo `rumoGraus` (°), ETA `etaMinutos` na SOG atual ("— " quando null). Atualiza a cada posição. Botão X limpa.
- [ ] **Step 3: Alarme de âncora** — botão "Fundeei" (só com posição): grava âncora na posição atual + raio (input 20-100m, default 40), círculo no mapa (source GeoJSON polygon aproximado), persiste em `localStorage` (recarregou → alarme continua armado). A cada posição: `foraDoRaio` → estado de alarme: banner vermelho pulsante "GARRANDO — verifique o fundeio", `navigator.vibrate([500,200,500])` repetido, e `new Notification("Commander — alarme de âncora", ...)` se `Notification.permission === "granted"` (pedir permissão ao armar). Desarmar limpa tudo. Honestidade no UI: "com o app aberto" (PWA não roda com tela trancada — texto `apoio text-dim` no armar).
- [ ] **Step 4: MOB** — botão vermelho discreto mas alcançável (canto, h-11): marca a posição ATUAL com marker próprio (`<Icone nome="alerta">` ou similar real), seta `destino` para ela imediatamente (rumo de volta na tela) e vibra. Confirmação de UM toque (MOB não pode ter fricção de duas etapas — comentar no código o porquê).
- [ ] **Step 5:** verificação + commit: `feat: navegacao ativa com rumo eta alarme de ancora e mob`

---

### Task 7: Costuras, docs e passe final

**Files:**
- Modify: `web/app/page.tsx` (landing: link "Para parceiros" no footer → `/parceiro`)
- Modify: `web/app/(app)/hoje/page.tsx` (se não houver, entrada para `/navegar` a partir do boletim do mar — grep antes; se já existe, não duplicar)
- Modify: `web/.env.example` (+`NEXT_PUBLIC_MAPBOX_TOKEN` comentado: onde criar), `docs/OPERACAO.md` (seção Mapbox: conta, token, free tier 50k loads/mês, atribuição OpenSeaMap CC-BY-SA), `docs/CONTRIBUTING.md` (passe visual: mapa + painel do parceiro)
- Modify: `web/middleware.ts` — NADA (o `/parceiro` exige login pelo comportamento default; confirmar apenas)

- [ ] **Step 1-3:** implementar acima; verificação final da onda: `npm test` 105, tsc, eslint, `npm run build` (rotas `/parceiro` e `/navegar`); grep confirma zero `setHTML` no código novo.
- [ ] **Step 4:** commit: `chore: costuras do mapa, env example e docs de operacao`
