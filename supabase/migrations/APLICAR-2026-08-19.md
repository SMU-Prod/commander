# Como aplicar as correções de 19/08/2026

Origem: `docs/auditoria/2026-08-19-banco-e-rls.md`.
Alvo: projeto Supabase `khgjtxvmduizyooqaoox` — **produção, sem staging**.

Nada disto foi executado. Os arquivos foram escritos a partir da **definição viva**
do banco (`pg_get_functiondef`, `pg_policies`, `pg_constraint`), não dos arquivos de
migration antigos — eles divergem, e essa divergência é o defeito que mais custou
tempo neste projeto.

## Antes de começar

1. Rode a **linha de base** abaixo e guarde o resultado. É com ela que você compara depois.

```sql
select
  (select count(*) from public.vinculos)                                   as vinculos,          -- 3
  (select count(*) from public.vinculos where suspenso_em is not null)     as suspensos,         -- 0
  (select count(*) from public.vinculos where papel = 'PROP')              as props,             -- 3
  (select count(*) from public.viagens)                                    as viagens,           -- 1
  (select count(*) from public.sondagens)                                  as sondagens,         -- 2
  (select count(*) from public.afazeres)                                   as afazeres,          -- 3
  (select count(*) from public.estoque_movimentos)                         as estoque_mov,       -- 1
  (select count(*) from public.tanque_movimentos)                          as tanque_mov,        -- 0
  (select count(*) from public.auditoria)                                  as auditoria,         -- 1
  (select count(*) from public.bases_operacionais)                         as bases,             -- 0
  (select count(*) from pg_policies where schemaname = 'public')           as policies;          -- 218
```

2. **Uma migration por vez.** Cada uma é um `begin; … commit;` — rode, confira, só então
   passe para a próxima. Não emende as sete.
3. Todas são idempotentes (`create or replace`, `drop policy if exists`). Reaplicar não quebra.
4. Depois de aplicar, rode o app e faça um login real. RLS não tem teste automatizado aqui:
   a conferência de que nada trancou é abrir uma embarcação e ver o diário, as fotos e o financeiro.

## Ordem de aplicação

| # | Arquivo | Fecha | Obrigatória? |
|---|---|---|---|
| 1 | `067_suspensao_vale_na_raiz.sql` | P0-1 | **Sim — é a mais urgente** |
| 2 | `068_viagens_e_sondagens_pela_matriz.sql` | P1-2, P1-3 | Sim |
| 3 | `069_afazeres_pela_matriz.sql` | P1-4 | Sim |
| 4 | `070_estoque_e_tanques_destino_com_vinculo.sql` | P1-5(b) | Sim |
| 5 | `071_auditoria_so_para_quem_administra.sql` | P1-7 | Sim |
| 6 | `072_decisao_vinculos_prop_nao_suspende.sql` | ressalva do P0-1 | **Decisão do dono** |
| 7 | `073_decisao_bases_operacionais_fk_restrict.sql` | P1-5(a), parte de integridade | **Decisão do dono** |

A ordem importa: a **067 tem de vir primeiro**, porque as migrations 068 a 071 apoiam-se
nos três helpers já corrigidos. Aplicar 068–071 antes da 067 deixa as policies novas
com a suspensão ainda inoperante.

As duas últimas estão fora do caminho crítico de segurança: são regra de negócio.
Leia o cabeçalho de cada uma e decida. **Aplicar 067–071 e parar é um estado consistente.**

---

## 1 · `067_suspensao_vale_na_raiz.sql`

Suspender alguém deixa de ser cosmético. Reescreve `permissao()`,
`pode_ver_embarcacao()` e `eh_prop()` para respeitarem `vinculos.suspenso_em`, e
corrige a policy de INSERT de `auditoria` (único outro ponto que consultava
`vinculos` na mão e concedia escrita a um suspenso).

**Confere (tem de voltar 3):**
```sql
select count(*) from pg_proc p
  join pg_namespace n on n.oid = p.pronamespace
 where n.nspname = 'public'
   and p.proname in ('permissao','pode_ver_embarcacao','eh_prop')
   and pg_get_functiondef(p.oid) ilike '%suspenso_em%';
```

**Confere que ninguém foi trancado (tem de voltar 0):**
```sql
select count(*) from public.vinculos where suspenso_em is not null;
```

**Confere que as três continuam DEFINER com search_path fixo (tem de voltar 3):**
```sql
select count(*) from pg_proc p
  join pg_namespace n on n.oid = p.pronamespace
 where n.nspname = 'public'
   and p.proname in ('permissao','pode_ver_embarcacao','eh_prop')
   and p.prosecdef
   and array_to_string(p.proconfig, ',') like '%search_path%';
```

**Teste de fumaça, com um login real:** abra uma embarcação e confirme que o diário,
as fotos, os documentos e o financeiro carregam normalmente. Se qualquer tela vier
vazia, reverta — é sinal de que algum vínculo tem `suspenso_em` inesperado.

**Reverter** (volta exatamente à definição que estava viva em 19/08/2026):
```sql
begin;

create or replace function public.permissao(emb uuid, aba text, modo text)
returns boolean language sql stable security definer set search_path to 'public'
as $function$
  select exists (
    select 1 from public.vinculos v
    where v.embarcacao_id = emb
      and v.usuario_id = auth.uid()
      and (
        v.papel = 'PROP'
        or coalesce((v.permissoes -> aba ->> modo)::boolean, false)
      )
  );
$function$;

create or replace function public.pode_ver_embarcacao(emb uuid)
returns boolean language sql stable security definer set search_path to 'public'
as $function$
  select exists (
    select 1 from public.vinculos v
    where v.embarcacao_id = emb and v.usuario_id = auth.uid()
  );
$function$;

create or replace function public.eh_prop(emb uuid)
returns boolean language sql stable security definer set search_path to 'public'
as $function$
  select exists (
    select 1 from public.vinculos v
    where v.embarcacao_id = emb and v.usuario_id = auth.uid() and v.papel = 'PROP'
  );
$function$;

drop policy if exists "auditoria: registra em nome proprio, na embarcacao que acessa" on public.auditoria;
create policy "auditoria: registra em nome proprio, na embarcacao que acessa"
  on public.auditoria for insert to authenticated
  with check (
    autor_id = auth.uid()
    and exists (select 1 from public.vinculos v
                where v.embarcacao_id = auditoria.embarcacao_id
                  and v.usuario_id = auth.uid())
  );

commit;
```

---

## 2 · `068_viagens_e_sondagens_pela_matriz.sql`

`viagens` sai de uma policy `FOR ALL` que só pedia vínculo e passa a quatro policies
pela matriz (`diario`). `sondagens` mantém leitura do barco inteiro, mas correção e
exclusão passam a ser de quem levantou.

**Confere (tem de voltar 8 linhas — 4 de cada, todas `{authenticated}`):**
```sql
select tablename, policyname, cmd, roles::text
  from pg_policies
 where schemaname = 'public' and tablename in ('viagens','sondagens')
 order by tablename, cmd;
```

**Confere que a viagem existente continua visível para o dono (tem de voltar 1):**
```sql
select count(*) from public.viagens v
 where exists (select 1 from public.vinculos x
               where x.embarcacao_id = v.embarcacao_id and x.papel = 'PROP');
```

**Teste de fumaça:** abrir o diário de bordo e ver a viagem listada; abrir a carta e
ver as sondagens.

**Reverter:**
```sql
begin;
drop policy if exists "viagens: ver pela matriz" on public.viagens;
drop policy if exists "viagens: criar pela matriz" on public.viagens;
drop policy if exists "viagens: atualizar pela matriz" on public.viagens;
drop policy if exists "viagens: excluir pela matriz" on public.viagens;
create policy "viagens: tudo com vinculo" on public.viagens
  for all using (pode_ver_embarcacao(embarcacao_id))
  with check (pode_ver_embarcacao(embarcacao_id));

drop policy if exists "sondagens: ve as do barco" on public.sondagens;
drop policy if exists "sondagens: grava a propria" on public.sondagens;
drop policy if exists "sondagens: corrige a propria" on public.sondagens;
drop policy if exists "sondagens: apaga a propria" on public.sondagens;
create policy "sondagens: dono grava e le as suas" on public.sondagens
  for all using (pode_ver_embarcacao(embarcacao_id))
  with check (pode_ver_embarcacao(embarcacao_id) and usuario_id = (select auth.uid()));
commit;
```

---

## 3 · `069_afazeres_pela_matriz.sql`

O INSERT passa a exigir vínculo com a embarcação declarada e responsável que seja
tripulante ativo daquela unidade (fecha a injeção de tarefa na lista de estranho).
O SELECT **amplia**: o PROP passa a enxergar as tarefas do próprio barco.

**Confere (4 policies, e o INSERT tem de citar `permissao` e `vinculos`):**
```sql
select policyname, cmd, roles::text, with_check
  from pg_policies where schemaname = 'public' and tablename = 'afazeres'
 order by cmd;
```

**Confere que as 3 tarefas de hoje continuam legíveis (tem de voltar 3):**
```sql
select count(*) from public.afazeres a
 where exists (select 1 from public.vinculos v
               where v.usuario_id = a.dono_id and v.embarcacao_id = a.embarcacao_id);
```

**Teste de fumaça:** abrir `/afazeres`, ver as tarefas, criar uma nova.
Se a criação falhar com `new row violates row-level security policy`, a causa é a
embarcação declarada não bater com a matriz de quem criou — investigue antes de reverter.

**Reverter:**
```sql
begin;
drop policy if exists "afazeres: o dono cria" on public.afazeres;
create policy "afazeres: o dono cria" on public.afazeres
  for insert to authenticated with check (dono_id = auth.uid());

drop policy if exists "afazeres: dono, responsavel e a unidade leem" on public.afazeres;
create policy "afazeres: dono e responsavel leem" on public.afazeres
  for select to authenticated
  using (dono_id = auth.uid() or responsavel_id = auth.uid());

drop policy if exists "afazeres: dono e responsavel atualizam" on public.afazeres;
create policy "afazeres: dono e responsavel atualizam" on public.afazeres
  for update to authenticated
  using (dono_id = auth.uid() or responsavel_id = auth.uid())
  with check (dono_id = auth.uid() or responsavel_id = auth.uid());

drop policy if exists "afazeres: so o dono apaga" on public.afazeres;
create policy "afazeres: so o dono apaga" on public.afazeres
  for delete to authenticated using (dono_id = auth.uid());
commit;
```

---

## 4 · `070_estoque_e_tanques_destino_com_vinculo.sql`

Consumo de peça e de combustível deixa de poder apontar para barco de outra conta.
Só mexe em INSERT — leitura e histórico ficam intactos.

**Confere (as duas policies de INSERT têm de citar `pode_ver_embarcacao`):**
```sql
select tablename, policyname, with_check
  from pg_policies
 where schemaname = 'public'
   and tablename in ('estoque_movimentos','tanque_movimentos')
   and cmd = 'INSERT';
```

**Confere que o movimento existente continua consistente (tem de voltar 1):**
```sql
select count(*) from public.estoque_movimentos em
 where em.embarcacao_id is null
    or exists (select 1 from public.vinculos v
               where v.embarcacao_id = em.embarcacao_id and v.usuario_id = em.autor_id);
```

**Teste de fumaça:** lançar uma saída de estoque e um abastecimento pelas telas
`/estoque` e `/combustivel`.

**Reverter:**
```sql
begin;
drop policy if exists "estoque_mov: dono do item registra" on public.estoque_movimentos;
create policy "estoque_mov: dono do item registra" on public.estoque_movimentos
  for insert to authenticated
  with check (
    autor_id = auth.uid()
    and exists (select 1 from public.estoque_itens i
                where i.id = estoque_movimentos.item_id and i.dono_id = auth.uid())
  );

drop policy if exists "tanque_mov: dono do tanque registra" on public.tanque_movimentos;
create policy "tanque_mov: dono do tanque registra" on public.tanque_movimentos
  for insert to authenticated
  with check (
    autor_id = auth.uid()
    and exists (select 1 from public.tanques t
                where t.id = tanque_movimentos.tanque_id and t.dono_id = auth.uid())
  );
commit;
```

---

## 5 · `071_auditoria_so_para_quem_administra.sql`

A trilha de auditoria deixa de ser legível por qualquer vinculado.
**É a única migration do lote que restringe leitura de verdade.**

**Confere (tem de voltar exatamente 2 linhas: 1 SELECT e 1 INSERT, nenhuma de UPDATE/DELETE):**
```sql
select policyname, cmd from pg_policies
 where schemaname = 'public' and tablename = 'auditoria' order by cmd;
```

**Confere que ninguém perdeu leitura hoje (as duas contagens têm de ser iguais — 3 e 3):**
```sql
select
  (select count(*) from public.vinculos) as vinculados_hoje,
  (select count(*) from public.vinculos where papel = 'PROP') as continuam_lendo;
```

**Teste de fumaça:** abrir a tela de histórico/auditoria da embarcação como PROP e
confirmar que a linha aparece.

**Reverter:**
```sql
begin;
drop policy if exists "auditoria: o dono e quem administra leem" on public.auditoria;
create policy "auditoria: quem tem acesso a embarcacao le" on public.auditoria
  for select to authenticated
  using (exists (select 1 from public.vinculos v
                 where v.embarcacao_id = auditoria.embarcacao_id
                   and v.usuario_id = auth.uid()));
commit;
```

---

## 6 · `072_decisao_vinculos_prop_nao_suspende.sql` — decisão do dono

Só aplique se a regra **"PROP nunca é suspensível"** for a pretendida.
Leia o cabeçalho do arquivo. Confirme antes (tem de voltar 0):

```sql
select count(*) from public.vinculos where papel = 'PROP' and suspenso_em is not null;
```

**Confere depois (1 linha):**
```sql
select conname, pg_get_constraintdef(oid) from pg_constraint
 where conname = 'vinculos_prop_nao_suspende';
```

**Reverter:**
```sql
alter table public.vinculos drop constraint vinculos_prop_nao_suspende;
```

---

## 7 · `073_decisao_bases_operacionais_fk_restrict.sql` — decisão do dono

Só aplique depois de decidir o fluxo de exclusão de conta: com RESTRICT, apagar um
perfil que tenha base operacional passa a **falhar** em vez de apagar a base junto.
Confirme antes (tem de voltar 0):

```sql
select count(*) from public.bases_operacionais;
```

**Confere depois (o def tem de terminar em `ON DELETE RESTRICT`):**
```sql
select conname, pg_get_constraintdef(oid) from pg_constraint
 where conname = 'bases_operacionais_dono_id_fkey';
```

**Reverter:**
```sql
alter table public.bases_operacionais
  drop constraint bases_operacionais_dono_id_fkey,
  add  constraint bases_operacionais_dono_id_fkey
       foreign key (dono_id) references public.profiles(id) on delete cascade;
```

---

## Conferência final, depois das cinco obrigatórias

```sql
-- Helpers respeitando suspensão: 3
select count(*) from pg_proc p join pg_namespace n on n.oid = p.pronamespace
 where n.nspname = 'public' and p.proname in ('permissao','pode_ver_embarcacao','eh_prop')
   and pg_get_functiondef(p.oid) ilike '%suspenso_em%';

-- Nenhuma tabela com RLS desligada: 0
select count(*) from pg_class c join pg_namespace n on n.oid = c.relnamespace
 where n.nspname = 'public' and c.relkind = 'r' and not c.relrowsecurity;

-- Nenhuma tabela sem policy: 0
select count(*) from pg_class c join pg_namespace n on n.oid = c.relnamespace
 where n.nspname = 'public' and c.relkind = 'r'
   and not exists (select 1 from pg_policies p
                   where p.schemaname = 'public' and p.tablename = c.relname);

-- Nenhuma função DEFINER sem search_path: 0
select count(*) from pg_proc p join pg_namespace n on n.oid = p.pronamespace
 where n.nspname = 'public' and p.prosecdef
   and coalesce(array_to_string(p.proconfig, ','), '') not like '%search_path%';

-- viagens não pode mais ter policy FOR ALL: 0
select count(*) from pg_policies
 where schemaname = 'public' and tablename = 'viagens' and cmd = 'ALL';
```

---

## O que este lote NÃO cobre

Registrado para não se perder — está detalhado na auditoria:

- **P1-6 · o link de cotista não tem como ser resgatado.** Não existe
  `aceitar_convite_cotista()`, e `aceitar_convite()` cria vínculo com `papel = 'CMDT'`
  fixo. Ninguém consegue entrar como COTISTA. Fechar isso exige decidir o desenho
  (vaga de cota, prazo do código) **e** mexer na rota `/convite/[codigo]` — não é só
  migration, e por isso ficou fora.
- **P1-5(a) · estoque, tanques e bases pertencem a uma pessoa, não à empresa.**
  Redesenho de produto.
- **P2-4 · os 15 índices de chave estrangeira** do BLOCO 7 da auditoria.
  `create index concurrently` não roda dentro de transação — se forem aplicados, tem de
  ser soltos, fora de migration transacional.
- **Fora do SQL:** ligar o *Leaked password protection*, confirmar que *Confirm email*
  está ligado (`gold_reivindicar_consultor()` confia no e-mail do JWT), e decidir sobre
  a idempotência dos contadores de publicidade.

---

## Nota sobre a numeração das migrations

O diretório tem 67 arquivos (`001` a `066`, com `033` duplicado em
`033_connect_interesses` e `033_gold`). O remoto tem **117 migrations aplicadas**. Os
números não batem porque **o mesmo arquivo foi aplicado fatiado em várias migrations
remotas** — `048_planos_assinatura` virou 8 partes, `049_admin_papeis` virou 7,
`050_avaliacoes_gold_porte` 5, `044_agenda` e `046_marketplace_demandas` 4 cada, e
`041_campos_finais_prd.sql` foi dissolvido em ~10 migrations nomeadas por assunto
(`contatos_empresa_email_observacoes`, `eventos_local_saida_passageiros`, …). Há também
nomes que divergem entre arquivo e remoto (`023_avisos_gerais.sql` →
`avisos_gerais_alertas_enviados`; `039_transferencia_embarcacao.sql` →
`038_transferencia_embarcacao`).

**Consequência prática:** `supabase db push` não é confiável aqui, e o histórico remoto
não reconstrói o diretório. Estas migrations seguem a numeração **do diretório**
(último real = `066_cotista_envios_e_afazeres.sql`, daí `067`), e devem ser aplicadas
**por SQL direto**, como as rodadas anteriores.
